# Unified config: parser + apps (nl2smt, solver). Use: smtparser --config PATH ...
# Sections: [parser] (options.h), [nl2smt] (env or short keys), [solver].
# NL2SMT: short keys (model, api_key, prompt_plan, ...) are mapped to env; relative prompt paths are resolved against this file's directory.

[parser]
logic=QF_LIA
# keep_let=true
# keep_division=true
# precision=128
# float_evaluate=true
# expand_functions=true
# expand_recursive_functions=false

[nl2smt]
# --- LLM (short keys or env names) ---
# NL2SMT_LLM_SCRIPT=/path/to/llm_call.py   # required for Tree-Plan→Emit→Repair
# NL2SMT_LLM_CMD=sh /path/to/apps/config/mock_llm.sh   # legacy single-shot / tests
# model = NL2SMT_MODEL   (or set NL2SMT_MODEL=...)
# api_key = OPENAI_API_KEY
# api_base = OPENAI_API_BASE
# NL2SMT_MODEL=gpt-4o-mini
# OPENAI_API_KEY=...
# OPENAI_API_BASE=...
# --- Prompts: paths relative to this config file, or absolute ---
# Default prompts live in apps/config/prompts/; use llm.conf in this dir for a full NL2SMT-only example.
# prompt_file   = prompts/prompt.txt
# prompt_plan   = prompts/prompt_plan.txt
# prompt_emit   = prompts/prompt_emit.txt
# prompt_repair = prompts/prompt_repair.txt

[solver]
# path=/usr/bin/z3
