Translate the following natural language into valid SMT-LIB2. Output ONLY SMT-LIB2. No markdown, no explanations.

Rules:
- Always output (set-logic <name>) first. Choose a logic that fits the problem, e.g. QF_LIA (integer linear), QF_LRA (real linear), QF_LIRA (mixed int/real), QF_BV (bitvectors), QF_UF (uninterpreted functions), QF_NIA/QF_NRA (nonlinear), or other SMT-LIB logics.
- Include (declare-fun ...) for each variable, (assert ...) for constraints, then (check-sat), (exit).
- If the problem asks to minimize or maximize a term, add (minimize x) or (maximize x) (or the expression) before (check-sat).

<<<USER_INPUT>>>
